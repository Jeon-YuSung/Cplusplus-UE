/*
C++는 컴파일 언어이자 정적(Statically typed)언어
모든 엔티티의 타입을 사용 시점에 컴파일러에 알려줘야함. 
객체의 타입에 따라 그 그 객체에 적용할 수 있는 연산 집합과 메모리 레이아웃이 달라짐.
*/

#include <iostream> //c++20에서는 import std;로도 가능 


int main(void) //메인함수이자 전역함수로 딱 하나만 포햄햐야함. 

{
	std::cout << "Hello, world!" << std::endl; // 출력문
	
	return 0; 
}

